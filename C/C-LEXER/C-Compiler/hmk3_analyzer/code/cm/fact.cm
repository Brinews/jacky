int z;
int x;
int m[100];

int chg(int a, int b, int c )
{
	int d;

	d = a + b + c ;

	return m[c+a];
}

int k;

int fact( int x)
/* recursive factorial functions */
{ if (x > 1)
	return x * fact(x-1);
  else
 	return 1;
}
int main(void)
{   int x;
    x = read();
    if (x >0) 
        write(fact(x));  
    return 0;
}

